# Supply-chain cooldown: only install package versions published at least 7 days ago.
# Locked installs (npm ci / package-lock.json) are unaffected; this guards NEW resolutions.
# Requires npm >= 11.6 (older npm prints a harmless "unknown config" warning and ignores it).
min-release-age=7
